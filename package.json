{
  "name": "portfolio-website",
  "version": "1.0.0",
  "private": true,
  "description": "Mohammad Zayed portfolio site",
  "scripts": {
    "build": "node build.mjs",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
